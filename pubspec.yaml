name: movieverse
description: A new Flutter project.

publish_to: 'none' # لمنع نشر الباكيج على pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  youtube_player_flutter: ^8.1.2
  cached_network_image: ^3.4.0
  provider: ^6.1.2
  animated_text_kit: ^4.2.2
  device_preview: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # تأكدي إن المجلدات موجودة في مشروعك حتى لو فاضية
 # assets:

  fonts:
    - family: Pacifico
      fonts:
        - asset: Fonts/Pacifico-Regular.ttf
